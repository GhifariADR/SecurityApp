#spring.application.name=hybridJwtToken
spring:
  application:
    name: securityApp
  datasource:
    url: jdbc:postgresql://localhost:5432/study
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

